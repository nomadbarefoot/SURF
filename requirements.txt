# SURF Browser Service - Core Dependencies

# ==============================================================================
# Core Framework (Required)
# ==============================================================================
fastapi==0.136.3
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.1

# ==============================================================================
# Browser Automation (Required)
# ==============================================================================
playwright==1.61.0
httpx==0.28.1
curl_cffi==0.15.0
adblock==0.6.0
mcp==1.27.2

# Optional compatibility backend for some Cloudflare-fronted pages.
# Install only when explicitly needed:
# cloudscraper>=1.2.71

# ==============================================================================
# Logging & Monitoring (Required)
# ==============================================================================
structlog==25.5.0
psutil==7.2.2

# ==============================================================================
# Caching (Optional - has in-memory fallback)
# ==============================================================================
# Redis is optional - falls back to in-memory cache if not available
# Uncomment if you want Redis caching support:
# redis>=5.0.1
# aioredis>=2.0.1
